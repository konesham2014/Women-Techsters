//defime arrays of a component and template based on their project structure

const componentNames = ['audio-card', 'button']
const templateNames = ['event-card', 'recently-played', 'tabs']

//loop through the component and template names
for (const name of componentNames) {
    console.log('Component: ${name}')
}

for (const name of templateNames) {
    consolr.log('Template: ${name}')
}